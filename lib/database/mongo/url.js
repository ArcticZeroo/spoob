const { mongo: config } = require('../../../config/database');
module.exports = `mongodb://${config.username}:${config.password}@${config.ip}:${config.port}${(config.database)? `/${config.database}` : ''}`;